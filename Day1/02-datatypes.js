//var username = "demosalesmanager";
var username = null; //Can be assigned with multiple values for a same variable name
console.log(username);
console.log("The datatype of username : "+typeof username);
